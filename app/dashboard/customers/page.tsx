import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customer Dashboard',
};

const Page = () => {
  return (
    <div>
      <p>Customer</p>
    </div>
  );
};

export default Page;
